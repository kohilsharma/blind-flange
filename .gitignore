node_modules/
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
dist/
build/
.DS_Store
Thumbs.db

# model weights and caches - never commit, always pre-staged separately
*.gguf
*.safetensors
models/
.cache/

# The prebuilt llama.cpp binaries the `local` model provider runs against
# (ADR-0001). MIT, but third-party and ~50 MB unpacked, so it is fetched
# rather than tracked - the same treatment the pinned harness gets.
vendor/

# The BMAD tool installation itself is NOT tracked — removed 28 Aug 2026.
#
# `.claude/skills/` is 49 third-party skill packages and `_bmad/` is the
# framework install: 265 files and about 3 MB of somebody else's code, shipping
# with no licence text of its own. This repository's central claim is that
# every component's licence is enumerated and checked, and `npm run licence-audit`
# covers npm, Python, the models and vendored binaries — it never looked at
# either of these trees. Publishing un-audited third-party code beside that
# claim is the one inconsistency this project cannot afford, and the tooling is
# reinstallable in a command, so it goes.
#
# Install BMAD separately if you want to run its workflows; nothing the
# workbench does at runtime depends on it.
#
# `_bmad/custom/` IS kept: those overrides are ours, they explain themselves,
# and docs/ralph-loop.md refers to them.
.claude/skills/
_bmad/*
!_bmad/custom/

# BMAD working output — tracked in full since 28 Aug 2026.
#
# It used to be that only the planning artifacts and the sprint tracker were
# kept, and everything under implementation-artifacts was treated as build
# scratch. That was right while this was a solo build: the specs were a record
# of work only one person would ever read.
#
# It stopped being right when the repository was handed to a teammate. The
# specs say why each story was built the way it was, deferred-work.md is the
# standing list of known problems and the reasoning behind each, and the epic
# contexts carry the constraints. Someone picking this up cold needs all of it,
# and 116 KB of markdown is a cheap price for not having to ask.
#
# `party-mode/` stays out: it is a scratch directory for an interactive
# brainstorming mode, not a record of anything the build depends on.
_bmad-output/party-mode/

# Claude Code worktrees — Lane B works in one; never commit a worktree tree.
.claude/worktrees/

# Story 5.4's approval-note tool writes real .docx files here at runtime
# (session cwd defaults to the repo root under `dsh --profile headless`).
# Generated output, never source.
/deliverables/
