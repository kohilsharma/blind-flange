{
  "name": "blind-flange",
  "version": "0.1.0",
  "private": true,
  "description": "Blind Flange (SIH26117) \u2014 a sovereign, air-gapped agentic AI workbench built on DeepSeek Harness. This manifest exists to carry the one documented start command; the workbench itself is the plugin package under plugins/ mounted into a dsh profile.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.15.0"
  },
  "scripts": {
    "start": "node scripts/start.mjs",
    "setup": "node scripts/start.mjs --setup-only",
    "test": "cd plugins/dsh-client-ui-base && node --test && cd ../.. && node scripts/licence-audit.mjs",
    "licence-audit": "node scripts/licence-audit.mjs",
    "record-demo": "node scripts/record-demo.mjs",
    "kiosk": "node scripts/kiosk.mjs",
    "doctor": "node scripts/doctor.mjs",
    "setup-ingestion": "node scripts/setup-ingestion.mjs",
    "ingestion": "node scripts/run-ingestion.mjs",
    "local-model": "vendor/llama.cpp/llama-server --models-dir models --models-max 2 --port 8790 -c 16384 -t 12 -ub 1024 -b 2048 --jinja --no-webui"
  }
}
